# 코드표준가이드 (Flutter) v1.0.0 📋
**Girls Band Tabi - Dart/Flutter 코딩 표준 및 Best Practices**

**최신 업데이트**: 2026년 2월 4일
**버전**: v1.0.0
**상태**: ✅ 최신
**작성**: Girls Band Tabi 개발팀

---

## 📋 목차

1. [개요](#1-개요)
2. [Dart/Flutter 코딩 스타일](#2-dartflutter-코딩-스타일)
3. [패키지 구조 규칙](#3-패키지-구조-규칙)
4. [네이밍 컨벤션](#4-네이밍-컨벤션)
5. [레이어별 코드 작성 규칙](#5-레이어별-코드-작성-규칙)
6. [문서화 표준](#6-문서화-표준)
7. [에러 처리 패턴](#7-에러-처리-패턴)
8. [로깅 패턴](#8-로깅-패턴)
9. [테스트 코드 표준](#9-테스트-코드-표준)
10. [코드 리뷰 체크리스트](#10-코드-리뷰-체크리스트)

---

## 1. 개요

### 1.1 목적
이 문서는 Girls Band Tabi Flutter 앱에서 **일관된 코드 품질**과
**유지보수성**을 확보하기 위한 표준을 정의합니다.

### 1.2 적용 범위
- Flutter(Dart) 앱 코드
- 위젯/상태관리/네트워크/로컬 저장소
- 테스트 코드

### 1.3 기본 원칙
1. **가독성 (Readability)**: 읽기 쉬운 코드
2. **일관성 (Consistency)**: 프로젝트 전역 통일
3. **단순성 (Simplicity)**: 불필요한 복잡성 최소화
4. **유지보수성 (Maintainability)**: 변경 비용 최소화

---

## 2. Dart/Flutter 코딩 스타일

### 2.1 언어/SDK 버전
- **Dart**: 3.x
- **Flutter**: 3.x 이상

### 2.2 포맷팅
- 들여쓰기: **스페이스 2칸**
- 줄 길이: **80~100자 내**
- 포맷터: `dart format` 결과를 신뢰

```dart
// 좋은 예: 2칸 들여쓰기
class PlaceRepository {
  Future<List<Place>> fetchAll() async {
    return [];
  }
}

// 나쁜 예: 탭 사용
class PlaceRepository {
	fetchAll() {}
}
```

### 2.3 Trailing Comma
```dart
// 좋은 예: 후행 쉼표 사용
const PlaceSummary({
  required this.id,
  required this.name,
  this.imageUrl,
});
```

### 2.4 Null Safety
```dart
// 좋은 예: null 안전 처리
final title = place.title ?? '제목 없음';

// 나쁜 예: 강제 언래핑 남용
final title = place.title!;
```

### 2.5 함수 스타일
```dart
// 좋은 예: 단일 표현식 함수
String formatTitle(String title) => title.trim();

// 복잡한 로직은 블록 사용
String resolveTitle(String? title) {
  if (title == null || title.isEmpty) return '제목 없음';
  return title.trim();
}
```

---

## 3. 패키지 구조 규칙

### 3.1 Feature-first + Clean Architecture
```
lib/
  features/
    places/
      presentation/
      application/
      domain/
      data/
  core/
    widgets/
    theme/
    routing/
    error/
```

### 3.2 레이어 책임
- **presentation**: UI 위젯, 화면, 라우팅
- **application**: 상태관리, 유스케이스, 컨트롤러
- **domain**: 엔티티, 인터페이스, 비즈니스 규칙
- **data**: DTO, Remote/Local DataSource, Repository 구현

---

## 4. 네이밍 컨벤션

### 4.1 클래스/위젯
```dart
class PlaceDetailPage extends StatelessWidget {}
```

### 4.2 변수/함수
```dart
final selectedPlaceId = '...';
void loadPlaces() {}
```

### 4.3 파일명
```
place_detail_page.dart
places_controller.dart
```

---

## 5. 레이어별 코드 작성 규칙

### 5.1 Presentation
- Stateless 위젯 우선
- 큰 build는 분리
- 상태는 상위로 올려 관리

### 5.2 Application
- Riverpod/BLoC 일관 사용
- 상태는 **immutable**
- side-effect는 분리

### 5.3 Domain
- 순수 Dart 코드
- 외부 의존성 금지

### 5.4 Data
- DTO ↔ Domain 변환 분리
- 네트워크/로컬 에러 매핑

---

## 6. 문서화 표준

### 6.1 주석 스타일 (영문 → 한글)
```dart
/// EN: Displays place detail page.
/// KO: 장소 상세 화면을 표시합니다.
class PlaceDetailPage extends StatelessWidget {}

// EN: Avoid rebuilds by keeping widget lightweight.
// KO: 불필요한 재빌드를 피하기 위해 위젯을 가볍게 유지합니다.
```

---

## 7. 에러 처리 패턴
- 공통 Failure 모델 사용
- UI는 스낵바/에러 위젯으로 일관 처리
- 네트워크 에러는 도메인 에러로 변환

---

## 8. 로깅 패턴
- 민감 정보 로그 금지 (토큰/비밀번호)
- 네트워크 로그는 개발 환경에서만

---

## 9. 테스트 코드 표준
- Unit/Widget/Integration 테스트 우선
- 중요한 상태 전이/네트워크 응답을 검증
- 테스트 이름은 행위 기반

---

## 10. 코드 리뷰 체크리스트

- [ ] `dart format` 적용됨
- [ ] 주석 EN/KO 규칙 준수
- [ ] Rebuild scope 최소화
- [ ] 에러 처리/빈 상태 포함
- [ ] 접근성(contrast/tap target) 고려
